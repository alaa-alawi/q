# Changelog

## Unreleased

- Initial publishable cleanup for `q`.
- Added REPL mode with shell/LLM routing, history, completions, sessions, MCP HTTP tools, local file tools, configurable system prompt, API logging, and streaming responses.
- Added build metadata and release target.
